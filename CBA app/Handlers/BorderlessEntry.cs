
using Microsoft.Maui.Controls;

namespace CBA_app.Handlers
{
    public class BorderlessEntry : Entry
    {
        //public static readonly BindableProperty BorderColorProperty =
        //   BindableProperty.Create(nameof(BorderColor), typeof(Color), typeof(BorderlessEntry), System.Drawing.Color.Gray);

        //public Color BorderColor
        //{
        //    get { return (Color)GetValue(BorderColorProperty); }
        //    set { SetValue(BorderColorProperty, value); }
        //}

        //public static readonly BindableProperty BorderWidthProperty =
        //    BindableProperty.Create(nameof(BorderWidth), typeof(int), typeof(BorderlessEntry), 1);

        //public int BorderWidth
        //{
        //    get { return (int)GetValue(BorderWidthProperty); }
        //    set { SetValue(BorderWidthProperty, value); }
        //}

        //public static readonly BindableProperty CornerRadiusProperty =
        //    BindableProperty.Create(nameof(CornerRadius), typeof(float), typeof(BorderlessEntry), 0f);

        //public float CornerRadius
        //{
        //    get { return (float)GetValue(CornerRadiusProperty); }
        //    set { SetValue(CornerRadiusProperty, value); }
        //}
    }
}
